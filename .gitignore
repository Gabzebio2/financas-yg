# ============================================================
# NUNCA subir para o GitHub — dados pessoais e arquivos locais
# ============================================================

# Prints e imagens (podem conter dados financeiros reais)
*.png
*.jpg
*.jpeg
*.webp

# Planilhas (as suas planilhas reais de finanças)
*.xlsx
*.xls
*.xlsm

# Backups exportados pelo app (contêm TODAS as suas transações)
*backup*.json
financas-yg-*.json

# Configurações locais do Claude Code e do Windows
.claude/
desktop.ini
Thumbs.db

# Segredos e artefatos de deploy — NUNCA subir (a chave da Anthropic
# fica só nas variáveis de ambiente da Vercel, jamais no repositório)
.env
.env.*
.vercel/
node_modules/

# Guias locais de uso/configuração (não são necessários no site)
LEIA-ME.txt
CONFIGURAR-SUPABASE.txt
ATIVAR-LEITURA-COMPROVANTE.txt
